# Cloud Function ignore file
# This overrides .gitignore for gcloud functions deploy
#
# Cloud Functions Gen2 runs `npm run build` (tsc) remotely,
# so src/ must be uploaded. dist/ is excluded since it will
# be rebuilt during the cloud build step.

# Include package-lock.json (needed for npm ci)
!package-lock.json

# Ignore development files
.git
.github
.vscode
.idea
*.md
!README.md
docs/
examples/
plans/

# Ignore test files
**/*.test.ts
**/*.spec.ts
**/*.test.js
**/*.spec.js
src/test/
coverage/
.nyc_output/

# Ignore local environment files
.env
.env.*
!.env.example

# Ignore build artifacts (rebuilt in cloud)
dist/
*.log

# Ignore Rust artifacts
target/
Cargo.lock
Cargo.toml
**/*.rs

# Ignore K8s configs
k8s/

# Ignore deployment configs (not needed inside the function)
deploy/
Dockerfile
docker-compose*.yml

# Ignore proto files
proto/

# Ignore node_modules (will be installed fresh)
node_modules/
